<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.codeboyq</groupId>
	<artifactId>AdminiMover</artifactId>
	<version>1.2.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>AdminiMover</name>
	<url>http://maven.apache.org</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<junit.version>4.8.2</junit.version>
		<commonsio.version>2.6</commonsio.version>
		<slf4j.version>1.7.25</slf4j.version>
		<logback.version>1.2.3</logback.version>
		<yaml.version>1.21</yaml.version>
		<maven.jar.plugin.version>3.1.0</maven.jar.plugin.version>
		<maven.clean.plugin.version>3.1.0</maven.clean.plugin.version>
		<maven.dependecy.plugin.version>3.1.1</maven.dependecy.plugin.version>
		<maven.resources.plugin.version>3.1.0</maven.resources.plugin.version>
		<maven.antrun.plugin.version>1.8</maven.antrun.plugin.version>
		<output.dir>dist</output.dir>
		<output.dir.app>${output.dir}/AdminiMoverApp</output.dir.app>
		<output.dir.service>${output.dir}/AdminiMoverService</output.dir.service>
		<output.dir.app.binaries>${output.dir.app}/_AdminiMoverBinaries</output.dir.app.binaries>
		<output.dir.service.binaries>${output.dir.service}/_AdminiMoverBinaries</output.dir.service.binaries>
		<temp.output.dir>target</temp.output.dir>
		<temp.output.dir.binaries>${temp.output.dir}/_AdminiMoverBinaries</temp.output.dir.binaries>
	</properties>

	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>${commonsio.version}</version>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-ext</artifactId>
			<version>${slf4j.version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>${logback.version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-core</artifactId>
			<version>${logback.version}</version>
		</dependency>
		<dependency>
			<groupId>org.yaml</groupId>
			<artifactId>snakeyaml</artifactId>
			<version>${yaml.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
			<version>3.0</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>

			<!-- Clean the output directory -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
				<version>${maven.clean.plugin.version}</version>
				<configuration>
					<followSymLinks>false</followSymLinks>
					<filesets>
						<fileset>
							<directory>${output.dir}</directory>
						</fileset>
						<fileset>
							<directory>${temp.output.dir}</directory>
						</fileset>
					</filesets>
				</configuration>
			</plugin>
			
			<!-- Copy the dependencies to a lib subfolder in the temporary output directory -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>${maven.dependecy.plugin.version}</version>
				<executions>
					<execution>
						<id>copy-dependencies</id>
						<phase>prepare-package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>
								${temp.output.dir.binaries}/libs
							</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Create the executable jar in the temporary output directory -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>${maven.jar.plugin.version}</version>
				<configuration>
					<archive>
						<manifest>
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
							<addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
							<addClasspath>true</addClasspath>
							<classpathPrefix>libs/</classpathPrefix>
							<mainClass>com.codeboyq.AdminiMover.MainApp</mainClass>
						</manifest>
					</archive>
					<outputDirectory>${temp.output.dir.binaries}</outputDirectory>
					<finalName>adminiMoverApp</finalName>
					<excludes>
						<exclude>**/*.yml</exclude>
					</excludes>
				</configuration>
			</plugin>
			
			<!-- Copy the resources to the temporary output directory -->
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>${maven.resources.plugin.version}</version>
				<executions>
					<execution>
						<id>copy-resources</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${temp.output.dir.binaries}</outputDirectory>
							<resources>
								<resource>
									<directory>src/main/resources</directory>
									<includes>
										<include>adminimover-config.yml</include>
										<include>logback.xml</include>
										<include>run.sh</include>
									</includes>
									<filtering>true</filtering>
								</resource>
							</resources>
						</configuration>
					</execution>
					<!-- Create the Automator Application folder and copy the temporary output directory -->
					<execution>
						<id>copy-resources-automator-app</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${output.dir.app}</outputDirectory>
							<resources>
								<resource>
									<directory>src/main/resources/macosx-automator/application</directory>
								</resource>
							</resources>
						</configuration>
					</execution>
					<execution>
						<id>copy-resources-automator-app-binaries</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${output.dir.app.binaries}</outputDirectory>
							<resources>
								<resource>
									<directory>${temp.output.dir.binaries}</directory>
								</resource>
							</resources>
						</configuration>
					</execution>
					<!-- Create the Automator Service folder and copy the temporary output directory -->				
					<execution>
						<id>copy-resources-automator-service</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${output.dir.service}</outputDirectory>
							<resources>
								<resource>
									<directory>src/main/resources/macosx-automator/service</directory>
								</resource>
							</resources>
						</configuration>
					</execution>	
					<execution>
						<id>copy-resources-automator-service-binaries</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${output.dir.service.binaries}</outputDirectory>
							<resources>
								<resource>
									<directory>${temp.output.dir.binaries}</directory>
								</resource>
							</resources>
						</configuration>
					</execution>																	
				</executions>
			</plugin>

			<!-- Grant Execute rights to the Automator Application, Service and the Shell script -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>${maven.antrun.plugin.version}</version>
				<executions>
					<execution>
						<id>process-test-classes</id>
						<phase>verify</phase>
						<configuration>
							<target>
								<chmod dir="${output.dir.app}/AdminiMover.app/Contents/MacOS/Application Stub" perm="755" />
								<chmod file="${output.dir.app.binaries}/run.sh" perm="755" />
								<chmod dir="${output.dir.service}/AdminiMover.app/Contents/MacOS/Application Stub" perm="755" />
								<chmod file="${output.dir.service.binaries}/run.sh" perm="755" />
							</target>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			

		</plugins>
	</build>
</project>
